<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Annuler une réservation – Hôtel Louvain</title>
  <link rel="stylesheet" href="/style.css" />
</head>
<body>
<div class="page">

  <header class="nav">
    <div class="nav-inner">
      <div class="nav-left">
        <a class="navlink" href="/">Accueil</a>
        <a class="navlink" href="/rooms">Chambres</a>
        <a class="navlink" href="/my-reservations">Mes réservations</a>
      </div>

      <div class="nav-right">
        <span class="pill"><%= user.username %></span>
        <a class="navlink" href="/logout">Déconnexion</a>
      </div>
    </div>
  </header>

  <main class="main">
    <div class="shell">
      <section class="panel">
        <h1 class="h1">Annulation de réservation</h1>

        <% if (error) { %>
          <div class="error"><%= error %></div>
        <% } %>

        <div class="card" style="margin-top:14px;">
          <div class="top">
            <h3><%= reservation.roomId ? reservation.roomId.name : "Chambre" %></h3>
            <span class="badge"><%= reservation.status %></span>
          </div>

          <p class="muted" style="margin-top:8px;">
            Arrivée : <strong><%= new Date(reservation.checkIn).toLocaleDateString('fr-BE') %></strong><br/>
            Départ : <strong><%= new Date(reservation.checkOut).toLocaleDateString('fr-BE') %></strong><br/>
            Invités : <strong><%= reservation.guests %></strong>
          </p>

          <p class="muted" style="margin-top:12px;">
            Règle : annulation possible uniquement si la date d’arrivée est dans plus de <strong>48h</strong>.
          </p>

          <% if (canCancel) { %>
            <form method="POST" action="/reservations/<%= reservation._id %>/cancel" style="margin-top:14px;">
              <button class="btn" type="submit">Confirmer l'annulation</button>
              <a class="navlink" style="margin-left:8px;display:inline-block;" href="/my-reservations">Retour</a>
            </form>
          <% } else { %>
            <div style="margin-top:14px;">
              <span class="muted">Annulation impossible : délai de 48h non respecté.</span>
              <div style="margin-top:10px;">
                <a class="btn" href="/my-reservations">Retour</a>
              </div>
            </div>
          <% } %>

        </div>
      </section>
    </div>
  </main>

  <footer class="footer">Hôtel Louvain</footer>
</div>
</body>
</html>
