<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Mes réservations – Hôtel Louvain</title>
  <link rel="stylesheet" href="/style.css" />
</head>
<body>
<div class="page">

  <header class="nav">
    <div class="nav-inner">
      <div class="nav-left">
        <a class="navlink" href="/">Accueil</a>
        <a class="navlink" href="/rooms">Chambres</a>
        <a class="navlink" href="/my-reservations">Mes réservations</a>
      </div>

      <div class="nav-right">
        <span class="pill"><%= user.username %></span>
        <a class="navlink" href="/logout">Déconnexion</a>
      </div>
    </div>
  </header>

  <main class="main">
    <div class="shell">
      <section class="panel">
        <h1 class="h1">Mes réservations</h1>

        <% if (!reservations || reservations.length === 0) { %>
          <p class="muted">Vous n'avez pas encore de réservation.</p>
          <div style="margin-top:12px;">
            <a class="btn" href="/rooms">Voir les chambres</a>
          </div>
        <% } else { %>

          <div class="table-wrap">
            <table class="res-table">
              <thead>
                <tr>
                  <th>Chambre</th>
                  <th>Arrivée</th>
                  <th>Départ</th>
                  <th>Invités</th>
                  <th>Statut</th>
                  <th class="action-col">Action</th>
                </tr>
              </thead>

              <tbody>
                <% reservations.forEach(r => { %>
                  <tr>
                    <td><%= r.roomId ? r.roomId.name : 'Chambre supprimée' %></td>
                    <td><%= new Date(r.checkIn).toLocaleDateString('fr-BE') %></td>
                    <td><%= new Date(r.checkOut).toLocaleDateString('fr-BE') %></td>
                    <td><%= r.guests %></td>
                    <td>
                      <span class="status <%= r.status === 'confirmed' ? 'status-ok' : 'status-cancel' %>">
                        <%= r.status %>
                      </span>
                    </td>

                    <!-- Action alignée à gauche -->
                    <td class="action-cell">
                      <% if (r.status === "confirmed") { %>
                        <a class="btn-cancel" href="/reservations/<%= r._id %>/cancel">Annuler</a>
                      <% } else { %>
                        <span class="muted">—</span>
                      <% } %>
                    </td>
                  </tr>
                <% }) %>
              </tbody>
            </table>
          </div>

          <p class="muted" style="margin-top:14px;">
            Annulation possible uniquement si la date d’arrivée est dans plus de 48h.
          </p>

        <% } %>

      </section>
    </div>
  </main>

  <footer class="footer">Hôtel Louvain</footer>
</div>
</body>
</html>
